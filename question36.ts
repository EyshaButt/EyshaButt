// Make Shirts

const makeShirt = (size: string, message: string): void =>
{
    console.log(`Size: ${size}, Message: ${message}`)
   
}

// Call the function
// call the function with the size and the message veriables.
let size: string = "medium"
let message: string = "TypeScript Insights with Eysha"

makeShirt(size, message)
// directly call the function
makeShirt("Large", "TypeScript Insights")


// make_Shirt("Medium", "TypeScript Insights with Eysha")







